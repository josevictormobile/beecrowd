#include <stdio.h>
 
int main() {
 
     int i=1, j=60;
            while (j >=0)
            {
                Console.WriteLine($"I={i} J={j}");
                i += 3;
                j -= 5;
            }
 
    return 0;
}